---
- name: Vérification de l'installation de Docker
  hosts: all
  become: true
  tasks:
    - name: Docker doit être installé
      command: docker --version
      register: result
      changed_when: false

    - name: Afficher la version de Docker
      debug:
        msg: "Docker version: {{ result.stdout }}"
